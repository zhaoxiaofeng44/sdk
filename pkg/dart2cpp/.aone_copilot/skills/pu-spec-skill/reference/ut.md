#### 4. `ut ${pu_id}` - 生成单元测试代码

**触发时机**：实现代码生成完成后

**执行标准**：
1. 🔴 读取 `{项目根目录}/puspec/changes/uc-xxx/pu-xxx/pu-xxx-change.impl.md` 作为唯一测试生成依据
2. 严格遵循 `{项目根目录}/puspec/project.md` 测试规范和编码规范
3. 生成`单元测试代码`，并确保可直接运行

**测试质量标准**：
- **测试覆盖率**（≥80%）：覆盖所有核心业务逻辑
- **场景完整性**（100%）：正常/异常/边界情况

**测试生成后验证**（MUST 强制执行）：
1. 🔴 **静态分析验证**：使用 read_lints 工具验证代码，确保无编译错误、语法错误
2. 🔴 **零容忍标准**：不允许出现任何编译报错、类型错误等低级失误
3. ✅ 使用编码规范清单逐项验证
4. ✅ 计算单测覆盖率（JaCoCo/Istanbul）
5. ✅ 向用户报告验证结果（目标覆盖率 ≥80%）

**输出**：`单元测试代码`

🔴 执行**B.2 核心执行原则**校验