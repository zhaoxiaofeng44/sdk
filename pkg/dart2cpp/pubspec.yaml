name: dart2cpp
description: Dart to C++ compiler and runtime library
version: 2.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dev_dependencies:
  lints: ^3.0.0

dependency_overrides:
  kernel:
    path: ../kernel
  _fe_analyzer_shared:
    path: ../_fe_analyzer_shared
